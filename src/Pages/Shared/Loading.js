import React from 'react';

const Loading = () => {
    return (
        <div>
            <button className="btn loading bg-indigo-500 border 0">loading</button>
        </div>
    );
};

export default Loading;